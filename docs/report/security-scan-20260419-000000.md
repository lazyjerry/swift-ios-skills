╔══════════════════════════════════════════════════════╗
║           AI 工具 資安掃描報告                       ║
╠══════════════════════════════════════════════════════╣
║  掃描時間：2026-04-19                                ║
║  掃描範圍：本機 ~/Dropbox/個人project/swift-ios-skills║
║  掃描項目：79 skills, 0 agents, 0 commands,          ║
║            0 rules, 0 agents_md, 1 plugin config     ║
╚══════════════════════════════════════════════════════╝

## 風險摘要

| 嚴重性   | 數量 |
|---------|------|
| Critical |  0  |
| High     |  0  |
| Medium   |  0  |
| Low      |  0  |

## 掃描覆蓋範圍

| 類別 | 項目 | 數量 |
|------|------|------|
| SKILL.md | `skills/` 下各子目錄 | 79 |
| references/ | 附屬參考文件 | 117 |
| Shell 腳本 | `.github/scripts/` | 2 |
| Plugin 設定 | `.claude-plugin/marketplace.json` | 1 |
| 授權 | PolyForm Perimeter 1.0.0 | 1 |

## 各項規則掃描結果

### ✅ A. 提示注入 — 無問題

- A1 覆寫系統指令：未偵測到
- A2 身份偽冒：未偵測到
- A3 隱藏指令：零寬字元未偵測到，無超過 10 行連續空白
- A4 觸發條件後門：未偵測到
- A5 權限聲稱：未偵測到

### ✅ B. 指令注入 — 無問題

- B1 未引號 Shell 變數：未偵測到
- B2 危險指令組合：`rm -rf` 出現於 `skills/debugging-instruments/SKILL.md` 第 247-248 行，內容為清理 SPM 快取（`rm -rf ~/Library/Caches/org.swift.swiftpm` 與 `rm -rf .build`），屬預期用途
- B3 任意代碼執行：`eval()` 出現於 `skills/apple-on-device-ai/SKILL.md`，為 PyTorch `model.eval()` 呼叫，非安全風險

### ✅ C. 資料外洩 — 無問題

- C1 讀取敏感憑證：`credentials` 出現於 `ios-security`、`authentication`、`callkit`、`app-store-review` 等 skill，均為教學 iOS Keychain / Sign in with Apple / App Review 的合法技術內容
- C2 外傳資料：未偵測到
- C3 環境資訊蒐集：未偵測到

### ✅ D. 權限升級 — 無問題

- D1 sudo：出現於 `skills/cryptotokenkit/SKILL.md` 第 146 行，為 CryptoTokenKit extension 註冊指令（`sudo -u _securityagent`），屬 Apple 官方 API 的預期用途
- D2 超出範圍存取：未偵測到
- D3 修改 AI 設定：未偵測到

### ✅ E. 社交工程 — 無問題

- E1 誘導信任：未偵測到
- E2 隱藏真實意圖：所有 SKILL.md 均含 `name` 與 `description` frontmatter，內容與描述一致

### ✅ F. 遙測與追蹤 — 無問題

- 無遙測、追蹤、或資料蒐集行為
- 無 `postinstall` hook 或安裝腳本
- `.github/scripts/` 的兩個腳本僅用於 CI（同步 issue template dropdown 選項、驗證 issue 格式），不涉及遙測

## Shell 腳本分析

| 腳本 | 用途 | 風險 |
|------|------|------|
| `.github/scripts/sync-skill-dropdowns.sh` | 同步 skills 目錄名稱到 issue template 的 dropdown 選項 | 無 |
| `.github/scripts/validate-issue-template.sh` | 驗證 issue body 是否符合 template 格式 | 無 |

兩個腳本均使用 `set -euo pipefail`，無網路請求、無寫入系統路徑、無環境變數外洩。

## 總結建議

✅ **建議安裝** — 此倉庫為純 Markdown 技術文件集，79 個 skill 均為 iOS/Swift 開發指引，無可執行程式碼、無遙測、無外部依賴。所有關鍵字命中均為合法技術教學內容。

> ⚠️ **提醒**：本報告由 AI 自動產生，結果可能存在誤判（false positive）或遺漏（false negative）。所有發現皆需經人工核實與驗證，不應作為唯一的安全評估依據。
