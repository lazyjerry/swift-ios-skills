╔══════════════════════════════════════════════════════╗
║           AI 工具 資安掃描報告                       ║
╠══════════════════════════════════════════════════════╣
║  掃描時間：2026-04-19                                ║
║  掃描範圍：本機 ~/Dropbox/個人project/swift-ios-skills║
║  掃描項目：79 skills, 0 agents, 0 commands,          ║
║            0 rules, 0 agents_md, 1 plugin config     ║
╚══════════════════════════════════════════════════════╝

## 風險摘要

| 嚴重性   | 數量 |
|---------|------|
| Critical |  0  |
| High     |  0  |
| Medium   |  0  |
| Low      |  0  |

## 掃描覆蓋範圍

| 類別 | 項目 | 數量 |
|------|------|------|
| SKILL.md | `skills/` 下各子目錄 | 79/79（全量） |
| references/ | 附屬參考文件 | 117（全量） |
| Shell 腳本 | `.github/scripts/` | 2（完整閱讀） |
| Plugin 設定 | `.claude-plugin/marketplace.json` | 1 |
| 授權 | PolyForm Perimeter 1.0.0 | 1 |

## 掃描方法

| 階段 | 覆蓋 | 方式 |
|------|------|------|
| Frontmatter 全量提取（name / description 一致性） | 79/79 | Python 腳本 |
| 關鍵字 pattern 掃描（規則 A-F） | 79 SKILL.md + 117 references | grep 全量 |
| 零寬字元偵測（A3） | 79/79 | Python 全量 |
| 連續空白行偵測（A3，>10 行） | 79/79 | Python 全量 |
| 語意可疑 pattern 掃描 | 79/79 prose 段落 | Python regex |
| 結構異常偵測（控制字元、Base64 隱藏、HTML 注釋、長行） | 79/79 | Python 全量 |
| Shell 腳本完整閱讀 | 2/2 | 人工閱讀 |

## 各項規則掃描結果

### ✅ A. 提示注入 — 無問題

- **A1 覆寫系統指令**：未偵測到
- **A2 身份偽冒**：未偵測到
- **A3 隱藏指令**：
  - 零寬字元：79 個檔案全量掃描，未偵測到
  - 連續空白行 >10：79 個檔案全量掃描，未偵測到
  - 控制字元（\x00-\x1f）：未偵測到
  - Base64 隱藏指令（prose 段落 >50 char）：未偵測到
  - HTML 注釋：4 個 skill 含 HTML 注釋（adattributionkit、background-processing、ios-security、sensorkit），全部位於 code block 內的 Info.plist XML 說明，無風險
- **A4 觸發條件後門**：未偵測到
- **A5 權限聲稱**：未偵測到

### ✅ B. 指令注入 — 無問題

- **B1 未引號 Shell 變數**：未偵測到
- **B2 危險指令組合**：`rm -rf` 出現於 `debugging-instruments/SKILL.md` L247-248，內容為清理 SPM 快取（`rm -rf ~/Library/Caches/org.swift.swiftpm`、`rm -rf .build`），預期用途
- **B3 任意代碼執行**：`eval()` 出現於 `apple-on-device-ai/SKILL.md`，為 PyTorch `model.eval()` 呼叫，預期用途

### ✅ C. 資料外洩 — 無問題

- **C1 讀取敏感憑證**：`credentials` 出現於 `ios-security`、`authentication`、`callkit`、`app-store-review`，均為教學 iOS Keychain / Sign in with Apple / App Review 合法技術內容
- **C2 外傳資料**：未偵測到
- **C3 環境資訊蒐集**：未偵測到

### ✅ D. 權限升級 — 無問題

- **D1 sudo**：出現於 `cryptotokenkit/SKILL.md` L146，為 CryptoTokenKit extension 的 Apple 官方註冊指令（`sudo -u _securityagent`），預期用途
- **D2 超出範圍存取**：未偵測到
- **D3 修改 AI 設定**：未偵測到

### ✅ E. 社交工程 — 無問題

- **E1 誘導信任**：未偵測到
- **E2 隱藏真實意圖**：79 個 SKILL.md 全量比對 frontmatter，name/description 與實際內容一致，無不符情形
  - `app-store-review` L117 命中 `bypass`（"IAP bypass" Apple 審核規則說明）：誤報
  - `app-store-review` L218 命中 `upload to`（上傳 App Store Connect）：誤報

### ✅ F. 遙測與追蹤 — 無問題

- 無遙測、追蹤、或資料蒐集行為
- 無 `postinstall` hook 或安裝腳本
- `.github/scripts/` 的兩個腳本僅用於 CI，不涉及遙測

## Shell 腳本分析

| 腳本 | 用途 | 風險 |
|------|------|------|
| `.github/scripts/sync-skill-dropdowns.sh` | 同步 skills 目錄名稱到 issue template dropdown | 無 |
| `.github/scripts/validate-issue-template.sh` | 驗證 issue body 格式 | 無 |

兩個腳本均使用 `set -euo pipefail`，無網路請求、無寫入系統路徑、無環境變數外洩。

## 誤報彙整

| 命中 | 位置 | 判定 | 說明 |
|------|------|------|------|
| `rm -rf` | debugging-instruments L247-248 | 誤報 | SPM/build 快取清理 |
| `eval()` | apple-on-device-ai | 誤報 | PyTorch model.eval() |
| `sudo` | cryptotokenkit L146 | 誤報 | Apple 官方 extension 註冊 |
| `jailbreak` | ios-security | 誤報 | iOS 越獄偵測防護教學 |
| `credentials` | ios-security、authentication 等 | 誤報 | Keychain/Sign in with Apple 教學 |
| `bypass` | app-store-review L117 | 誤報 | "IAP bypass" Apple 審核規則 |
| `upload to` | app-store-review L218 | 誤報 | 上傳 App Store Connect |
| HTML 注釋 | adattributionkit、background-processing、ios-security、sensorkit | 誤報 | 全在 code block 內的 Info.plist XML 注釋 |
| 長行 (L3) | 多個 skill | 誤報 | frontmatter description 欄位較長 |
| Scope boundary 長行 | swiftui-patterns L26 | 誤報 | skill 範圍說明文字 |

## 總結建議

✅ **建議安裝** — 79 個 SKILL.md 全量掃描，無任何安全疑慮。純 iOS/Swift 技術文件，無遙測、無可執行程式碼、無注入風險。所有命中均為誤報。

> ⚠️ **提醒**：本報告由 AI 自動產生，結果可能存在誤判（false positive）或遺漏（false negative）。所有發現皆需經人工核實與驗證，不應作為唯一的安全評估依據。
